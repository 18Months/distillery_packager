language: elixir
elixir:
  - 1.6.4
otp_release:
  - 19.0

script:
  - MIX_ENV=test mix deps.get
  - MIX_ENV=test mix test
after_script:
  - MIX_ENV=test mix coveralls.travis

dist: trusty
sudo: required
